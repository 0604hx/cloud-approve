# 打包问题集锦

### knex 动态依赖各类数据库驱动

配置 rsbuild 忽略用不到的数据库驱动：
```js
/**
 * 使用 Objection.js 和 Knex 时，Knex 会尝试加载其他数据库的依赖（如 pg、mysql 等）
 * 即使你只使用了 sqlite3。这些数据库驱动默认在 Knex 的代码中被 require，
 * 但未实际安装，导致在使用打包工具（如 rsbuild）时报错
 */
externals:['pg', 'pg-query-stream','oracledb', 'tedious', 'sqlite3', 'mysql']
```

### svg-captcha 字体文件
打包后会提示找不到`Comismsh.ttf`文件，这是因为代码写死的路径，解决方案有：

1. 将字体文件放置在正确的路径（入口文件的上层）
2. 修改代码 `node_modules\svg-captcha\lib\option-manager.js`
```js
const fontPath = path.join(
    __dirname,
    process.env.NODE_ENV === 'production'?
        './assets/Comismsh.ttf'
        :
        '../fonts/Comismsh.ttf'
);
```
### setupDB 方法报错 TypeError: r(...) is not a function
这是因为`require('knex')`返回的 default，需要转换：
```js
const { default : KnexBuilder, Knex } = require('knex')
```

### Could not locate the bindings file. Tried:

```shell
Knex: run
$ npm install sqlite3 --save
Could not locate the bindings file. Tried:
 → E:\workspace\github\cloud-approve\server\build\node_sqlite3.node
 → E:\workspace\github\cloud-approve\server\build\Debug\node_sqlite3.node
 → E:\workspace\github\cloud-approve\server\build\Release\node_sqlite3.node
 → E:\workspace\github\cloud-approve\server\out\Debug\node_sqlite3.node
 → E:\workspace\github\cloud-approve\server\Debug\node_sqlite3.node
 → E:\workspace\github\cloud-approve\server\out\Release\node_sqlite3.node
 → E:\workspace\github\cloud-approve\server\Release\node_sqlite3.node
 → E:\workspace\github\cloud-approve\server\build\default\node_sqlite3.node
 → E:\workspace\github\cloud-approve\server\compiled\22.7.0\win32\x64\node_sqlite3.node
 → E:\workspace\github\cloud-approve\server\addon-build\release\install-root\node_sqlite3.node
 → E:\workspace\github\cloud-approve\server\addon-build\debug\install-root\node_sqlite3.node
 → E:\workspace\github\cloud-approve\server\addon-build\default\install-root\node_sqlite3.node
 → E:\workspace\github\cloud-approve\server\lib\binding\node-v127-win32-x64\node_sqlite3.node
```

这个错误通常是因为 `sqlite3` 或其他原生模块（如 better-sqlite3）在打包过程中丢失了必要的二进制文件（bindings）。这些二进制文件在运行时需要加载，但打包工具（如 rsbuild）默认不会包含它们。

解决思路是打包时，将必要的`.node`文件拷贝出来，然后告之工具库使用。我没研究如何在`sqlite3`中实现，而是选择了`better-sqlite3`：

1. 在代码中指定[nativeBinding参数](https://github.com/WiseLibs/better-sqlite3/blob/7eeab2f6bb1b4a90b52b4af208aadc10db0517af/lib/database.js#LL36C14-L36C14)

```js
if(process.env.NODE_ENV === 'production'){
    connection.options = {
        nativeBinding: "assets/better_sqlite3.node"
    }
}
```
2. 配置 rsbuild 拷贝文件
```js
output:{
    copy:[{ 
            from: "./node_modules/better-sqlite3/build/Release/better_sqlite3.node", 
            to:"assets"
    }]
}
```
